# Form2Dashboard runs entirely in the browser.
# No API keys or server secrets are required for the default demo.

# Optional: public site URL used by some hosts / OG tooling
# NEXT_PUBLIC_SITE_URL=https://form2dashboard.vercel.app
