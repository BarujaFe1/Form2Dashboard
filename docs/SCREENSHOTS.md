# Screenshot capture guide

Synthetic fixtures only — never upload real customer CSVs with PII into screenshots.

## Required shots (refresh after this branch)

| File | Scene | Notes |
|---|---|---|
| `public/screenshots/upload.png` | Landing with both demo buttons | Include “CSV bagunçado” |
| `public/screenshots/mapper.png` | Column mapping after messy fixture | Show auto-detect banner |
| `public/screenshots/dashboard.png` | KPIs + charts + transform report | Scroll so profiling chips are visible |
| `public/screenshots/dark-mode.png` | Same dashboard in dark mode | |
| `public/screenshots/transform-report.png` *(optional)* | Cropped transform report card | Good LinkedIn/card asset |

## Capture checklist

1. Use fixture demo (no real PII).
2. Light mode primary for README hero.
3. 1440×900 or similar desktop viewport.
4. Hide personal browser chrome if possible.
5. Update README image paths if filenames change.

## Current status

Existing PNGs predate the transform-report and dual demo buttons. Replace before marketing the 1.1.0 story widely; until then, README notes the local demo as source of truth.
