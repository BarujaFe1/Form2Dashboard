/** @type {import('next').NextConfig} */
const nextConfig = {
  // Client-side demo: no special experimental flags required.
  // Keep config minimal for predictable Vercel builds.
};

export default nextConfig;
